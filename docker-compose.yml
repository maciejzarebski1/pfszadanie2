version: "3"
services:
  client:
    image: "susamogu/zad2:fib-client"
    mem_limit: 128m
    hostname: client
  server:
    image: "susamogu/zad2:fib-server"
    mem_limit: 128m
    hostname: api
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - PGUSER=postgres
      - PGHOST=postgres
      - PGDATABASE=postgres
      - PGPASSWORD=postgres
      - PGPORT=5432
  worker:
    image: "susamogu/zad2:fib-worker"
    mem_limit: 128m
    hostname: worker
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
  nginx:
    image: "susamogu/zad2:fib-nginx"
    mem_limit: 128m
    hostname: nginx
    ports:
      - "80:80"
